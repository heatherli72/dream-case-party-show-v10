# Dream Case Party Show

A static interactive presentation published with GitHub Pages.

Open `index.html` locally, or visit the GitHub Pages URL after deployment. The
presentation, including its tutorial media, is self-contained in `index.html`.
